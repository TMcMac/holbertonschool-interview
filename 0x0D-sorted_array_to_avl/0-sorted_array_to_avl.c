#include "binary_trees.h"
/**
 * sorted_array_to_avl - converts an array to an AVL tree
 *
 * @array: pointer to zeroth position of an array of integers
 * @size: the size of the array
 *
 * Return: a pointer to the root node of an AVL tree
 */

avl_t *sorted_array_to_avl(int *array, size_t size)
{
  if array == NULL || size <= 0
		{
		  return(NULL);
		}
  return(NULL);
}
